" Only start on demand
let g:instant_markdown_autostart = 0

" I don't need to preview every single keystroke. It just makes it slow to
" type... Preview on wait, save or when leaving insert mode
let g:instant_markdown_slow = 1
