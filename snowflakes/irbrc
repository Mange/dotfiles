# RVM ruins $IRBRC because it wants to inject extra stuff; then it is hardcoded
# to only look for ~/.irbrc.
# I should open a PR some day...
#
begin
  path = File.join(ENV["XDG_CONFIG_HOME"], "ruby", "irbrc")
  load path if File.exist?(path)
rescue LoadError => load_error
  puts load_error
rescue => exception
  puts "Error : 'load #{path}' : #{exception.message}"
end
