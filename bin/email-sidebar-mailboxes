#!/bin/sh

box="$1"

[ -z "$box" ] && exit 1

mailboxes="$HOME/Mail/$box"

find "$mailboxes" -mindepth 1 -type d | \
  grep -v "tmp\|new\|cur" | \
  sed "s|${mailboxes}/|=\"|g" | \
  sed 's|$|\"|g' | \
  tr '\n' ' '
