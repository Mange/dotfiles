#!/bin/sh
# Use git-delta if it exists, otherwise use normal less. This way I can have a
# shared global git config that won't crash on me before Delta is installed (or
# if I just don't install it).

if hash delta 2>/dev/null; then
  exec delta --dark "$@"
else
  exec less "$@"
fi
