# vim: ft=sh

if [[ $_mange_xdg_zealotry != "yes" ]]; then
  _mange_xdg_zealotry="yes"

  set -a
  # shellcheck source=../environment.d/10-xdg-zealotry.conf
  . "$HOME/.config/environment.d/10-xdg-zealotry.conf"
  set +a

  alias tmux='tmux -f "${XDG_CONFIG_HOME}/tmux/tmux.conf"'
fi
