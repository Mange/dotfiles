# Generated
config/user-dirs.dirs

# Allow machine-local files that are not added to the repo
*.local

# Some apps will create cache/data inside the config
config/nvim/.netrwhist
config/nvim/spell
config/beets/state*

# Project files
.luacheckcache
